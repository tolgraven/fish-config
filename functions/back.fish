function back
	contall
    ssh paj "tdtool --on screen"
    caffeinate -d -t 1
    sleep 5
    brightness refresh
    #whatelse?
    #not speakers on bc what if sound
    #that's where a sound level meter comes into play hehe
    #would be sweet for everything from waking up to evenings to whabla
end
