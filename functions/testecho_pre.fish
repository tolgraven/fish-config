function testecho_pre --no-scope-shadowing
	#echo -n "HIT" $argv "jajajo "
    #echo (string replace --all "\\" "ass" $argv)

    #commandline --current-buffer --replace (string replace --all "\\" "boo" $argv)
end
